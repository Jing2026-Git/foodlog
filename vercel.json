{
  "framework": null,
  "buildCommand": "mkdir -p output && cp static/index.html output/",
  "outputDirectory": "output",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}

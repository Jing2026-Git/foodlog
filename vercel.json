{
  "framework": null,
  "buildCommand": "mkdir -p public && cp static/index.html public/",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
